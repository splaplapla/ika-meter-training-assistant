<script>
jcrop = Jcrop.attach('target_dataset', {
  multiMin: 0,
  multi: true
})

function loadJcrop(points) {
  const options = {};
  let rect = null; 

  points.forEach(function(point) {
    jcrop.newWidget(
      Jcrop.Rect.create(point.x, point.y, point.width, point.height),
      options
    );
  })
};

function saveCrops() {
  Array.from(jcrop.crops).each(function(crop) {
    // cropをparamsに変換する
  })
  return true;
}
</script>
